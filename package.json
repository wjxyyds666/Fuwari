{
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build:cf": "PLATFORM=cloudflare astro build", // 构建Cloudflare适配产物（main分支用）
    "build:gh": "PLATFORM=github astro build",   // 构建GitHub适配产物（gh-pages分支用）
    "preview": "astro preview",
    "astro": "astro"
  }
}
